/*
Это контроллер для новогодней гирлянды. 
У гирлянды есть лампочки четырёх цветов, 
которыми управляют 4 соответствующих выхода у этого чипа.

Контроллер должен зажигать по одному цвету, 
и чередовать цвета по порядку, так, чтобы каждый цвет горел 
delay тиков. 

Вот пример работы чипа:

| time |delay|red|green|blue|yellow|
| 1    |   1 | 1 |  0  | 0  |  0   |
| 2    |   1 | 0 |  1  | 0  |  0   |
| 3    |   2 | 0 |  0  | 1  |  0   |
| 4    |   2 | 0 |  0  | 1  |  0   |
| 5    |   2 | 0 |  0  | 0  |  1   |
| 6    |   2 | 0 |  0  | 0  |  1   |
| 7    |   2 | 1 |  0  | 0  |  0   |
| 8    |   2 | 1 |  0  | 0  |  0   |
| 9    |   2 | 0 |  1  | 0  |  0   |
| 10   |   2 | 0 |  1  | 0  |  0   |
...
*/
CHIP NYLights {
    IN delay[16];
    OUT red, green, blue, yellow;

    PARTS:
    //будем использовать для отслеживания delay цикла
    Clock(period=delay, reset=false, ticks=none, loop=newColour);
    Bit(in=newValue0, load=true, out=value0);
    Bit(in=newValue1, load=true, out=value1);

    //добавляем 1 к sel
    HalfAdder(a=value0, b=newColour, sum=newValue0, carry=carry);
    Xor(a=value1, b=carry, out=newValue1);

    DMux4Way(in=true, sel[0]=newValue0, sel[1]=newValue1, a=red, b=green, c=blue, d=yellow);
}