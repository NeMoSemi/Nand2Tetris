//чтобы умножить R0 на R1 будем прибавлять к R2 R0 R1 раз
//Ставим итератор и в R1
@R1
D = A
@i
M = D 
@R2
M = D

(LOOP)
	//здесь кладём в D значение R0 и прибавляем его к R2
    @R0
    D = M
    @R2
	M = D + M
	//после увеличения R2 вычтем из i единицу, и нам останется прибавить R0 к R2 ещё i раз
    @i
	DM = M - 1
	//отправляемся в начало цикла loop, если i != 0
    @LOOP
    D;JGT


(END)
    @END
    0;JMP